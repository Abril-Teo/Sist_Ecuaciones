#pragma once
#include"Matrices.h"
double determinanteSarrus(const Matrix& A);
// Calcula el determinante de cualquier matriz cuadrada por Expansión de Laplace (recursivo).
double determinanteLaplace(const Matrix& A);